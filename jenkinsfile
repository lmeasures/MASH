pipeline {
    agent {
        // Reference a kubernetes pod template label
        label 'node'
        // Use inline kubernetes agent 
        // kubernetes { ... }
    }
    stages {
        stage('build') {
            steps {
                sh 'npm run dev'
           }
        }
    }
}